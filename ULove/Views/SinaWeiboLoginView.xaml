<Page
    x:Class="ULove.Views.SinaWeiboLoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ULove.Views"
    xmlns:control="using:ULove.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <WebView x:Name="webView"  ></WebView>
        <ProgressRing x:Name="progressRing" Width="50" Height="50" IsActive="True" Foreground="{StaticResource ULoveDefaultBrush}" ></ProgressRing>
        <control:NotificationBar x:Name="notificationBar"></control:NotificationBar>
    </Grid>
</Page>
